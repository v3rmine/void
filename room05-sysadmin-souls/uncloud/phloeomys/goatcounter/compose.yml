services:
  goatcounter:
    container_name: goatcounter
    image: arp242/goatcounter:latest
    pull_policy: always
    restart: unless-stopped
    user: 1000:1000
    volumes:
      - goatcounter-data:/home/goatcounter/goatcounter-data
    x-ports:
      - stats.v3rm.in:8080/http
      - stats.astriiid.fr:8080/http
    x-machines:
      - phloeomys

volumes:
  goatcounter-data:
