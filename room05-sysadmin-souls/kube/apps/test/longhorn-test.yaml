apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-test-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn # This should be the default Longhorn StorageClass
  resources:
    requests:
      storage: 1Gi
